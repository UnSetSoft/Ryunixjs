# Deprecated: lint runs in .github/workflows/ci.yml
# Kept only so open PRs referencing this file are not surprised; scheduled scan removed.

name: ESLint (legacy)

on:
  workflow_dispatch:

jobs:
  redirect:
    name: Use CI workflow instead
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "ESLint and the full test suite run in the CI workflow (.github/workflows/ci.yml)."
          echo "Trigger that workflow or open a PR against canary."
